# NVIDIA NIM API key (required for nim backend)
NVIDIA_API_KEY=your_api_key_here

# Optional: override default data directory (~/.virosense)
# VIROSENSE_DATA_DIR=/path/to/data

# Optional: override embedding cache directory
# VIROSENSE_CACHE_DIR=/path/to/cache
